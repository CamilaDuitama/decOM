{% set name = "decOM" %}
{% set version = "0.0.27" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: c1d6739670a2200869d1d8ac70900f675dba44d4cdf8cb87fa4ab28b87a5b86f

build:
  number: 0
  entry_points:
    - decOM = decOM.__main__:main
    - decOM-aOralOut = decOM.aOralOut.__main__:main
    - decOM-format = decOM.format.__main__:main
    - decOM-LOO = decOM.LOO.__main__:main
    - decOM-CV = decOM.CV.__main__:main
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - colorama
    - dask >=2021.12.0
    - importlib_resources >=5.4.0
    - python-kaleido
    - pip
    - plotly >=5.5.0
    - python
  run:
    - colorama
    - dask >=2021.12.0
    - importlib_resources >=5.4.0
    - python-kaleido
    - plotly >=5.5.0
    - python

test:
  imports:
    - decOM
    - decOM.CV
    - decOM.LOO
    - decOM.aOralOut
    - decOM.data
    - decOM.format
    - decOM.modules
    - test
  commands:
    - decOM --help
    - decOM-aOralOut --help
    - decOM-format --help
    - decOM-LOO --help
    - decOM-CV --help

about:
  home: "https://github.com/CamilaDuitama/decOM"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "decOM: Similarity-based microbial source tracking for contamination assessment of ancient oral samples using k-mer-based methods"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - CamilaDuitama
